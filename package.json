{
  "name": "@tktb-tess/my-zod-schema",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "UNLICENSED",
  "files": [
    "dist"
  ],
  "main": "./dist/bundle.js",
  "types": "./dist/main.d.ts",
  "exports": {
    "types": "./dist/main.d.ts",
    "import": "./dist/bundle.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "build-pack": "npm run build && rm -r -f pkg && mkdir pkg && npm pack --pack-destination pkg"
  },
  "devDependencies": {
    "@tktb-tess/my-zod-schema": "file:pkg/tktb-tess-my-zod-schema-0.1.0.tgz",
    "@types/node": "^24.3.1",
    "typescript": "~5.9.2",
    "vite": "^7.0.6",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "zod": "^4.1.5"
  }
}
